# CLAUDE.md

이 파일은 Claude Code가 이 프로젝트를 작업할 때 참고하는 가이드입니다.

## 프로젝트가 뭐야?

인터랙티브한 아트워크 웹페이지입니다. 빌드 도구나 프레임워크 없이 **순수 HTML/CSS/JS**로만 만들어졌어요.

- **왼쪽 패널**: 기하학적인 도형으로 그린 케이크. 초의 불꽃(촛불)을 클릭하면 켜지고 꺼져요.
- **오른쪽 패널**: 도면(blueprint) 느낌의 에디토리얼 레이아웃. ASCII 아트 배경, 여러 개의 "뷰포트"(작은 도면 박스), 클릭하면 켜지는 번호 스티커들이 있어요.
- **전역 효과**: 커스텀 커서(십자선), 마우스를 따라다니는 잔상(trail), 클릭하면 퍼지는 물결(ripple).

전체 톤은 민트/세이지 그린 + 흰 배경의 미니멀한 설계도면 스타일입니다.

## 파일 구조

```
index.html        — HTML 구조 (SVG로 그린 케이크, 오른쪽 도면 레이아웃)
style.css         — 모든 스타일과 CSS 애니메이션
script.js         — 모든 인터랙션 로직
viewport-01~07.svg — 참고용 SVG 에셋 (현재 페이지에 직접 쓰이진 않음)
```

**중요**: CSS는 `style.css`에, JS는 `script.js`에 둡니다. index.html 안에 `<style>`이나 `<script>` 블록을 다시 인라인으로 넣지 마세요. (예전에 인라인으로 섞여 있던 걸 분리한 프로젝트입니다.)

## 실행 방법

빌드 과정이 없습니다. 그냥 브라우저에서 `index.html`을 열면 돼요.
바뀐 걸 확인하려면 파일 저장 후 브라우저 새로고침만 하면 됩니다.

## 코드를 건드릴 때 알아둘 것

### 색상
일관된 팔레트를 씁니다. 새 요소를 추가할 때도 이 색을 쓰세요.
- 민트: `#7bc8bc` (주 강조색)
- 세이지 그린: `#8aad8a` (보조 강조색)
- 검정: `#111` (선/텍스트)
- 흰 배경: `#ffffff`

### 케이크 (index.html 왼쪽)
- `#cake-svg` 안에 SVG 도형들로 그려져 있어요.
- 케이크 몸통의 점박이 무늬(dot-matrix)는 `script.js`의 `generateDots()`가 자동으로 생성합니다. 점을 직접 HTML에 넣지 마세요.
- 촛불은 `#flame-g`(불꽃), `#smoke-g`(연기), `#pulse-g`(불 꺼졌을 때 퍼지는 링)로 구성됩니다. `toggleFlame()`이 이 셋의 표시 여부를 전환해요.

### 스티커 (index.html 오른쪽)
- 번호 스티커는 `01`~`07`까지 있고, 클릭하면 `handleStickerClick()`이 `.active` 클래스를 토글합니다.
- 새 스티커를 추가하려면: HTML에 `sticker-c-08`, `sticker-t-08` 형식으로 id를 맞춰서 추가하면 자동으로 작동해요 (함수가 id를 조립해서 찾기 때문).

### 애니메이션
- CSS 애니메이션(`@keyframes`)은 전부 `style.css`에 있습니다: 물결, 촛불 흔들림, 연기, 펄스 링 등.
- 마우스 잔상과 커서 이동은 JS(`requestAnimationFrame`)로 처리합니다.

## 작업할 때 권장 사항

- 새 CSS/JS는 항상 `style.css` / `script.js`에 추가하고, 기존 섹션 주석(`/* ─── 제목 ─── */`) 스타일을 따라 정리하세요.
- SVG 좌표를 다룰 일이 많으니, 위치를 바꿀 땐 `cx`/`cy`/`x`/`y`/`points` 값을 직접 조정하면 됩니다.
- 외부 라이브러리(jQuery, React 등)를 추가하지 마세요. 이 프로젝트는 의존성 없이 가볍게 유지하는 게 목표예요.
